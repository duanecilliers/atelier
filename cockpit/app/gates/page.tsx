import { ComingSoon } from '@/components/ComingSoon';

export default function GatesPage() {
  return (
    <ComingSoon
      title="Gates"
      phase="Phase 3"
      blurb="The deterministic checkpoints rendered with per-check evidence — pass/fail from gate_results, the WHAT behind each verdict, and the retry visualization. The UI observes the gates; it never bypasses them."
    />
  );
}
