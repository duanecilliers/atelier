import { ComingSoon } from '@/components/ComingSoon';

export default function SkillsPage() {
  return (
    <ComingSoon
      title="Skills"
      phase="Phase 4"
      blurb="A card grid read live from the factory's own cookbooks and recipes — the capability library the agents draw on."
    />
  );
}
