import { ComingSoon } from '@/components/ComingSoon';

export default function CostPage() {
  return (
    <ComingSoon
      title="Cost"
      phase="Phase 3"
      blurb="Per-run and per-model spend, tokens and latency summed across retries — the observability that lets you measure the thousandth run, therefore improve it."
    />
  );
}
